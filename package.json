{
  "name": "harri-eCommerce-backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "typescript": "^5.0.4"
  },
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only ./src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node ./dist/server.ts"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
